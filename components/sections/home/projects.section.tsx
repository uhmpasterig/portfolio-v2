'use client';
import { ProjectList } from '@/components/elements/project';

export const ProjectSection = () => {
  return (
    <section className="w-full mb-10">
      <ProjectList />
    </section>
  );
};
